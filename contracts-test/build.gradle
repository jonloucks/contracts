plugins {
    id 'java-library'
    id 'maven-publish'
    id 'jacoco'
    id 'jacoco-report-aggregation'
}

apply from: "${rootDir}/gradle/java-test-library.gradle"

dependencies {
    api project(':contracts-api')
}
