pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'contracts'
include 'contracts-api'
include 'contracts-test'
include 'contracts-impl'
include 'contracts-smoke'