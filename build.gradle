plugins {
    id 'java-library'
    id 'maven-publish'
    id 'signing'
}

apply from: "${rootDir}/gradle/java-library.gradle"

//tasks.withType(Sign).configureEach {
//    onlyIf { rootProject.ext.doingSigning }
//}

//project.tasks.publish.enabled = false


repositories {
    mavenCentral()
}

dependencies {
}
